# 变更记录

记录 RestoFlow 项目基线的所有变更。约定：按时间正序追加，格式为 日期 + 变更内容。

## 2026-08-21

- 确立文档体系：docs/ 下 8 份文档，逐份讨论确认后定稿；文件名采用英文短横线命名
- 新建 project-overview.md：项目说明，确立 V1 范围（堂食核心闭环）、暂缓清单（外卖/会员/优惠券/打印/交接班）、4 条设计原则
- 修订 project-overview.md：明细状态补充退菜；桌台增加"待清台"状态（结账与清台分离为两个动作）；新增术语表；服务员与收银员合并为同一角色；后端 Java 版本 17 改为 21；设计原则第 4 条补充落地方式；暂缓清单移除"多门店"

## 2026-08-22

- 确定 V1 权限方案：业务模块级访问控制，首页固定展示全部业务模块，动态菜单与按钮级权限归后续扩展
- 修订 project-overview.md：3.1 第 7 条改为"按角色控制业务模块的访问（模块级权限）"；3.2 备注同步（"完整权限管理"调整为"动态菜单、按钮级权限"归后续阶段）
- 修订 requirements-analysis.md：新增角色-模块权限矩阵（固定配置）；AUTH-03 删除与 AUTH-04 重叠的验收④；AUTH-04③ 改为"进入该功能时被系统拒绝"；ORDER-04 新增验收⑤（仅待清台可反结账）；"工作台"统一为"首页"
- 修订 project-overview.md：服务员职责删"退款"、店长职责删"营业报表"；管理员职责补充"可访问全部业务模块"；3.1 第 7 条权限表述对齐 AUTH-03；设计原则 4 文件名改为 CHANGELOG.md
- 修订 requirements-analysis.md：权限矩阵由 7 模块简化为 5 模块（点餐随桌台、结账随订单校验），与首页模块清单对齐
- 新建 business-process.md：业务流程文档初稿（3 个状态机、按角色组织的核心流程、8 个异常边界场景、需求追溯表）
- 新增工作规则：编写/修改文档时必须与已定稿文档交叉对照（冲突/歧义检查）；写代码必须遵循文档；用户最新指令优先，如有冲突暂停执行并提出讨论
- 新建 system-design.md：系统架构设计初稿（总体架构、技术选型、后端分层与模块划分、前端结构、关键技术决策、运行形态、架构约束）
- 确定架构决策：MyBatis-Plus；NF-04 刷新采用轮询；引入 knife4j；后端负责最终权限校验（前端仅展示优化）；架构原则"技术决策优先满足 V1 闭环，不为未来扩展引入复杂方案"
- 修订 AUTH-02（requirements-analysis.md）：登出语义限定为"当前客户端"登录状态失效；同步修订 system-design.md 对应技术决策，消除与 JWT 无状态特性的表述张力
- 修订 system-design.md：技术选型表 knife4j 行表述修正（API文档 / 接口文档生成与在线调试）；system-design.md 经评审定稿
- 新增工作规则：存在多种可选方案时，写出推荐方案并简要说明理由
- 新建 database-design.md：数据库设计初稿（9 张表；订单表命名 orders 避开 MySQL 保留字）
- 补充"订单状态与明细状态分工"说明（business-process.md 2.2、database-design.md 4.6）：退菜仅影响 order_item，不改变 orders.status；订单无"已退"状态
- 修订 database-design.md：order_operation_log 增加 remark 字段（VARCHAR(255) 可空，后端生成动作摘要，提升订单详情操作记录的信息量）；角色字段维持 user.role（讨论结论：不拆角色表，固定权限矩阵下拆表不解耦权限判断）；database-design.md 经评审定稿
- 新建 api-design.md：API 接口设计初稿（34 个接口；通用约定含 HTTP 状态码语义表、code 预留说明；数据对象约定 12 个）
- 修订 api-design.md：明确取消结账"当前有效支付记录"的精确定义；currentOrderId 明确为 API 组装字段、禁止持久化；补充多模块复用的权限语义（权限≠资源归属）；新增事务约定（2.8，多表写入接口事务内完成）；金额全部后端计算、状态码前端集中枚举映射
- 六文档交叉审查修复（8 项）：新增优惠/抹零接口（#10，PAY-02 补 API 落点）；反结账目标状态按明细分流（0 元无明细订单回"已开台"）；反结账与取消结账均清除优惠并重算应收；退菜时 payable 同步重算且低于已收则拒绝；KDS 仅返回未结账订单明细；project-overview 3.2 措辞改"不预留字段"；system-design 补充停用用户 token 简化说明（涉及 business-process / database-design / api-design / system-design / project-overview 五份文档）
- 新建 development-plan.md：开发路线初稿（7 个阶段：初始化 → 认证 → 基础数据 → 桌台点餐 → KDS → 订单结账 → 全链路验收；垂直切片开发、手工验收为主）
- 修订 development-plan.md（采纳外部评审意见）：阶段 0/1 职责重划（JWT 与权限拦截移入阶段 1）；阶段 1 拆为 1a 认证能力 + 1b 用户维护，阶段 2 依赖降为 1a；明确基础能力切片与业务垂直切片的区别；阶段 3 增加内部开发顺序 3A~3D（按事务边界拆）；阶段 5 增加开发前核对状态转换表与金额单一真相提醒；阶段 6 验收改五类场景（正常/异常/边界/权限/状态转换，不引入并发幂等）；每阶段增加"本阶段不做"清单；新增"开发前置检查"章节
- 修订 api-design.md：轮询约定精确化为 T1−T0 ≤ 5 秒（NF-04），前端轮询统一封装 usePolling

## 2026-08-30

- 修订 development-plan.md（文档闭环一致性审查修复 2 项）：阶段 3 验收由 TABLE-01~04 改为 TABLE-01~03，清台（TABLE-04）验收统一归阶段 5；阶段 2 接口补充 #2 桌台列表、#7 菜品列表（基础数据维护页复用），阶段 3 相应移除 #2/#7 并调整 3A 为复用阶段 2 已实现接口
